<?php $format = $this->format;
if($format != 'mobile'){
	print $this->partial('template/portal_nav.phtml'); 
} else { 
	?><p class="left" style="margin: 1.2em 0 0 0; font-size: 0.9em;"><?php
$pages = array(
    array(
        'label'      => 'Start',
        'controller' => 'index',
    	'module'     => 'website',
    	'action'	 => 'index',
    	'pages'      => array(
            array(
                'label'      => 'Portal',
                'module'     => 'website',
                'controller' => 'portal',
            	'action'	 => 'index',
	            'pages'      => array(
		            array(
		                'label'      => 'Barkunde',
		                'module'     => 'website',
		                'controller' => 'portal',
		            	'action'	 => 'barkunde',
		            	'pages'      => array(
				            array(
				                'label'      => 'Originalrezepte',
				                'module'     => 'website',
				                'controller' => 'portal',
				            	'action'	 => 'authors'
				            )
			        	)
		            )
	        	)
            )
        )
    )
);
$container = new Zend_Navigation($pages);
echo str_replace('">','?format=mobile">',$this->navigation($container)->breadcrumbs());
?></p>
	<?php 
}?>
<div class="content">
<h2>Original Cocktailrezepte</h2>
<h3>Cocktailrezept-Autoren</h3>
<?php
$options = array(	'controller'=>'recipe',
					'action'=>'index',
					'search_type'=>'author',
					'search'=>'*');
?>
<ul>
	<li><a
		href="<?php 
		$options['search'] = urlencode('Schumann'); 
		print $this->url($options); ?>?format=<?php print $format; ?>">Charles Schumann</a>:
	<div style="margin-top: 1em;"><?php foreach($this->recipes['schumann'] as $recipe){ 
		print $this->cocktailPreview($recipe);
	} ?></div>
	</li>
	<li><a
		href="<?php
		$options['search'] = urlencode('Brandl');
		print $this->url($options); ?>?format=<?php print $format; ?>">Franz Brandl</a>:
	<div style="margin-top: 1em;"><?php foreach($this->recipes['brandl'] as $recipe){ 
		print $this->cocktailPreview($recipe);
	} ?></div>
	</li>
	<li><a
		href="<?php
		$options['search'] = urlencode('Beach');
		print $this->url($options); ?>?format=<?php print $format; ?>">Donn Beach</a>:
	<div style="margin-top: 1em;"><?php foreach($this->recipes['beach'] as $recipe){ 
		print $this->cocktailPreview($recipe);
	} ?></div>
	</li>
	<li><a
		href="<?php
		$options['search'] = urlencode('Trader Vic');
		print $this->url($options); ?>?format=<?php print $format; ?>">Trader Vic</a>:
	<div style="margin-top: 1em;"><?php foreach($this->recipes['trader_vic'] as $recipe){ 
		print $this->cocktailPreview($recipe);
	} ?></div>
	</li>
</ul>
<h3>Cocktailbücher und andere Quellen</h3>
<ul>
<?php
$options['search_type'] = 'source';
foreach($this->sources as $source) { ?>
	<li><a
		href="<?php
		$options['search'] = urlencode($source);
		print $this->url($options); ?>?format=<?php print $format; ?>"><?php print $source; ?></a></li>
		<?php } ?>
</ul>
</div>
